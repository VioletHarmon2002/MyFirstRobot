version: '3.8'

services:
  little-endian:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - ./web:/var/www/html
    restart: always
